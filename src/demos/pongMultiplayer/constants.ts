import { Vector2 } from "three";

export const GAME_SIZE = new Vector2(640, 480);
export const PADDLE_SIZE = new Vector2(15, 100);
export const PADDLE_SPEED = 300;
export const BALL_START_SPEED = 100;
export const BALL_SPEED_UP = 20;
export const BALL_SIZE = 10;
export const PADDLE_OFFSET = 280;
export const MAX_SCORE = 3;